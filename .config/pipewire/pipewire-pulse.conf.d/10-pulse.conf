# /qompassai/dotfiles/.config/pipewire/pipewire-pulse.conf.d/10-pulse.conf
# Qompass AI PipeWire-Pulse Config
# Copyright (C) 2026 Qompass AI, All rights reserved
#########################################################################
# References: #   https://docs.pipewire.org/page_man_pipewire-pulse_conf_5.html |
# https://docs.pipewire.org/page_module_protocol_pulse.html | https://man.archlinux.org/man/pipewire-pulse.conf.5.en
pulse.cmd = [
    { cmd                                                  = "load-module"  args = "module-always-sink"     flags = []
      condition                                            = [ { pulse.cmd.always-sink      = !false } ] }
    { cmd                                                  = "load-module"  args = "module-device-manager"  flags = []
        condition                                          = [ { pulse.cmd.device-manager   = !false } ] }
    { cmd                                                  = "load-module"  args = "module-device-restore"  flags = []
        condition                                          = [ { pulse.cmd.device-restore   = !false } ] }
    { cmd                                                  = "load-module"  args = "module-stream-restore"  flags = []
        condition                                          = [ { pulse.cmd.stream-restore = !false } ] }
    { cmd                                                  = "load-module"  args = "module-gsettings" flags = [ nofail ] }
    { cmd                                                  = "load-module"  args = "module-switch-on-connect" }
]
pulse.properties = {
    pulse.allow-module-loading                             = true
    pulse.default.format                                   = F32
    pulse.default.frag                                     = 96000/48000
    pulse.default.position                                 = [ FL FR ]
    pulse.default.req                                      = 960/48000
    pulse.default.tlength                                  = 96000/48000
    pulse.idle.timeout                                     = 0
    pulse.min.frag                                         = 256/48000
    pulse.min.quantum                                      = 256/48000
    pulse.min.req                                          = 256/48000
    server.address                                         = [ "unix:native" ]
    server.dbus-name                                       = "org.pulseaudio.Server"
}
pulse.properties.rules = [
    {
        matches                                            = [ { cpu.vm.name = !null } ]
        actions = {
            update-props = {
                pulse.min.quantum                          = 1024/48000
            }
        }
    }
]
pulse.rules = [
    {
        matches = [
            {
                # all keys must match the value. ! negates. ~ starts regex.
                #client.name                = "Firefox"
                #application.process.binary = "teams"
                #application.name           = "~speech-dispatcher.*"
            }
        ]
        actions = {
            update-props = {
                #node.latency = 512/48000
            }
            # Possible quirks:
            #    force-s16-info                 forces sink and source info as S16 format
            #    remove-capture-dont-move       removes the capture DONT_MOVE flag
            #    block-source-volume            blocks updates to source volume
            #    block-sink-volume              blocks updates to sink volume
            #quirks = [ ]
        }
    }
    {
        # block-source-volume: prevent apps from controlling source/capture volume
        matches = [
            { application.process.binary = "chrome" }
            { application.process.binary = "chromium" }
            { application.process.binary = "code" }
            { application.process.binary = "obsidian" }
            { application.process.binary = "zoom" }
            { application.name           = "~.*[Zz]oom.*" }
        ]
        actions = { quirks = [ block-source-volume ] }
    }
    {
        # discord needs both block-source-volume and remove-capture-dont-move
        matches = [
            { application.process.binary = "discord" }
            { application.process.binary = "Discord" }
            { application.name           = "~.*[Dd]iscord.*" }
            { application.name           = "WEBRTC VoiceEngine" }
        ]
        actions = { quirks = [ block-source-volume remove-capture-dont-move ] }
    }
    {
        # firefox marks the capture streams as don't move and then they
        # can't be moved with pavucontrol or other tools.
        matches = [ { application.process.binary = "firefox" } ]
        actions = { quirks = [ remove-capture-dont-move ] }
    }
    {
        # skype/teams do not want to use devices that don't have an S16 sample format.
        matches = [
            { application.process.binary = "skypeforlinux" }
            { application.process.binary = "teams" }
            { application.process.binary = "teams-for-linux" }
            { application.process.binary = "teams-insiders" }
        ]
        actions = { quirks = [ force-s16-info ] }
    }
    {
        # obs needs minimum latency tuning to avoid underruns
        matches = [ { application.process.binary = "obs" } ]
        actions = {
            update-props = {
                pulse.min.req     = 512/48000
                pulse.min.quantum = 512/48000
            }
        }
    }
    {
        # speech dispatcher asks for too small latency and then underruns.
        matches = [ { application.name = "~speech-dispatcher.*" } ]
        actions = {
            update-props = {
                pulse.idle.timeout = 5          # pause after 5 seconds of underrun
                pulse.min.quantum  = 512/48000  # 10.6ms
                pulse.min.req      = 512/48000  # 10.6ms
            }
        }
    }
]
