# Context property rules (VM detection, etc.)
context.properties.rules = [
    {   matches = [ { cpu.vm.name = !null } ]
        actions = {
            update-props = {
                # Higher latency in VMs
                default.clock.min-quantum = 1024
            }
        }
    }
]
