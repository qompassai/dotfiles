screen:
{
    ## window_min = ambient_br - contrib;                                                                        #
    ## window_max = ambient_br + contrib;                                                                        #
    ## bl_new = window_max - (0.4 * screen_br)                                                                   #

    # disabled = true;
    # contrib = 0.2;
    # timeouts = [ 5, -1 ];
};
