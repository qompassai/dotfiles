# /qompassai/dotfiles/.config/wireplumber/monitor_alsa.conf
# Qompass AI Wireplumber Alsa Monitor Config
# Copyright (C) 2026 Qompass AI, All rights reserved
# ----------------------------------------
monitor.alsa.rules = [
  {
    matches = [
      { device.name = "~alsa_card.*" }
    ]
    actions = {
      update-props = {
        api.alsa.use-acp = true
        api.alsa.use-ucm = false
      }
    }
  }
  {
    matches = [
      { node.name = "audiorelay-virtual-mic-sink" }
    ]
    actions = {
      update-props = {
        "priority.driver"  = 2000
        "priority.session" = 2000
        "node.description" = "AudioRelay Virtual Mic (preferred)"
        "media.role"       = "communication"
      }
    }
  }
]
