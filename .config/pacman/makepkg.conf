#!/hint/bash
# shellcheck disable=2034
## /qompassai/dotfiles/.config/pacman/makepkg.conf
# Qompass AI Pacman Makepkg Config
# Copyright (C) 2025 Qompass AI, All rights reserved
####################################################
#BUILDDIR=/tmp/makepkg
BUILDENV=(!distcc color !ccache check !sign)
CARCH="x86_64"
CC="sccache clang"
CXX="sccache clang++"
CHOST="x86_64-pc-linux-gnu"
CFLAGS="-march=x86-64 -mtune=native -O2 -pipe -fno-plt -fexceptions \
  -Wp,-D_FORTIFY_SOURCE=3 -Wformat -Werror=format-security \
  -fstack-clash-protection -fcf-protection \
  -fno-omit-frame-pointer -mno-omit-leaf-frame-pointer "
COMPRESSBZ2=(lbzip2 -c -f)
COMPRESSGZ=(pigz -c -f -n)
COMPRESSLRZ=(lrzip -q)
COMPRESSLZ=(lzip -c -f)
COMPRESSLZ4=(lz4 -q)
COMPRESSLZO=(lzop -q)
COMPRESSXZ=(xz -c -z -T0 -)
COMPRESSZ=(compress -c -f)
COMPRESSZST=(zstd -c -T0 --ultra -3 -)
CXXFLAGS="$CFLAGS -Wp,-D_GLIBCXX_ASSERTIONS "
CPPFLAGS=""
DEBUG_CFLAGS="-g"
DEBUG_CXXFLAGS="-g"
#DISTCC_HOSTS=""
DLAGENTS=('file::/usr/bin/curl -qgC - -o %o %u'
          'ftp::/usr/bin/curl -qgfC - --ftp-pasv --retry 3 --retry-delay 3 -o %o %u'
          'http::/usr/bin/curl -qgb "" -fLC - --retry 3 --retry-delay 3 -o %o %u'
          'https::/usr/bin/curl -qgb "" -fLC - --retry 3 --retry-delay 3 -o %o %u'
          'rsync::/usr/bin/rsync --no-motd -z %u %o'
          'scp::/usr/bin/scp -C %u %o')

DOC_DIRS=(usr/{,local/}{,share/}{doc,gtk-doc} opt/*/{doc,gtk-doc})
GOFLAGS="-tags=release -trimpath"
GPGKEY="FF3DF12925AA841877AE6864BA8D40BF292A6CD9"
INTEGRITY_CHECK=(sha256 sha384 sha512)
LDFLAGS="-Wl,-O1 -Wl,--sort-common -Wl,--as-needed -Wl,-z,relro -Wl,-z,now \
         -Wl,-z,pack-relative-relocs -fuse-ld=mold"
LIB_DIRS=('lib:usr/lib' 'lib32:usr/lib32' 'lib64:usr/lib64')
LTOFLAGS="-flto=auto"
MAKEFLAGS="-j$(nproc)"
MAN_DIRS=({usr{,/local}{,/share},opt/*}/{man,info})
OPTIONS=(autodeps strip docs !libtool !staticlibs emptydirs zipman purge debug lto )
PKGEXT='.pkg.tar.zst'
PURGE_TARGETS=(usr/{,share}/info/dir .packlist *.pod)
RUSTFLAGS="-C target-cpu=native -C opt-level=3 -C force-frame-pointers=yes"
SRCEXT='.src.tar.gz'
STRIP_BINARIES="--strip-all"
STRIP_SHARED="--strip-unneeded"
STRIP_STATIC="--strip-debug"
VCSCLIENTS=('bzr::breezy'
            'fossil::fossil'
            'git::git'
            'hg::mercurial'
            'svn::subversion')

# vim: set ft=sh ts=2 sw=2 et:
