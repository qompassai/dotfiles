# Generated by confgen.sh
# Module: v4l2loopback
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/v4l2loopback/0.15.4/7.2.6-zen2-1-zen/x86_64/module/v4l2loopback.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options v4l2loopback devices=1 video_nr=10 card_label="LinuxBroadcast" exclusive_caps=1 max_buffers=2
options v4l2loopback devices=4
options v4l2loopback card_label="Hyprland Virtual Camera","Hyprland Screen Capture","Iriun Webcam","Iriun Webcam #2" 
options v4l2loopback exclusive_caps=1,1,1,1
options v4l2loopback max_buffers=4
options v4l2loopback max_width=3840
options v4l2loopback max_height=2160
options v4l2loopback video_nr=10,11,12,13

# Available parameters from: modinfo -p v4l2loopback
# debugging level (higher values == more verbose) (int)
# options v4l2loopback debug=<value>

# how many buffers should be allocated [DEFAULT: 2] (int)
# options v4l2loopback max_buffers=<value>

# how many users can open the loopback device [DEFAULT: 10] (int)
# options v4l2loopback max_openers=<value>

# how many devices should be created (int)
# options v4l2loopback devices=<value>

# video device numbers (-1=auto, 0=/dev/video0, etc.) (array of int)
# options v4l2loopback video_nr=<value>

# card labels for each device (array of charp)
# options v4l2loopback card_label=<value>

# whether to announce OUTPUT/CAPTURE capabilities exclusively or not  [DEFAULT: 0] (array of bool)
# options v4l2loopback exclusive_caps=<value>

# maximum allowed frame width [DEFAULT: 8192] (int)
# options v4l2loopback max_width=<value>

# maximum allowed frame height [DEFAULT: 8192] (int)
# options v4l2loopback max_height=<value>

