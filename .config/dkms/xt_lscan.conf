# Generated by confgen.sh
# Module: xt_lscan
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/xtables-addons/3.31/7.2.6-zen2-1-zen/x86_64/module/xt_lscan.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p xt_lscan
# only set specified bits in connection mark (uint)
# options xt_lscan connmark_mask=<value>

# only set specified bits in packet mark (uint)
# options xt_lscan packet_mask=<value>

# nfmark value for packet-seen state (uint)
# options xt_lscan mark_seen=<value>

# connmark value for SYN Received state (uint)
# options xt_lscan mark_synrcv=<value>

# connmark value for closed state (uint)
# options xt_lscan mark_closed=<value>

# connmark value for SYN Scan state (uint)
# options xt_lscan mark_synscan=<value>

# connmark value for Established-1 state (uint)
# options xt_lscan mark_estab1=<value>

# connmark value for Established-2 state (uint)
# options xt_lscan mark_estab2=<value>

# connmark value for Connect Scan state (uint)
# options xt_lscan mark_cnscan=<value>

# connmark value for Grab Scan state (uint)
# options xt_lscan mark_grscan=<value>

# connmark value for Valid state (uint)
# options xt_lscan mark_valid=<value>

