# Generated by confgen.sh
# Module: scst
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/scst-modules/3.10.5.7r0/7.2.6-zen2-1-zen/x86_64/module/scst.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p scst
# Enables a run-time ALUA state invariant check. (default: false) (bool)
# options scst alua_invariant_check=<value>

# SCSI target threads count (int)
# options scst scst_threads=<value>

# Maximum memory allowed to be consumed by all SCSI commands of all devices at any given time in MB (int)
# options scst scst_max_cmd_mem=<value>

# Maximum memory allowed to be consumed by all SCSI commands of a device at any given time in MB (int)
# options scst scst_max_dev_cmd_mem=<value>

# If enabled, close the sessions associated with an access control group (ACG) when an ACG is deleted via sysfs instead of returning -EBUSY. (default: false) (bool)
# options scst forcibly_close_sessions=<value>

# Enables the copy managers auto registration. (default: true) (bool)
# options scst auto_cm_assignment=<value>

# If enabled, LUN replace operations do not wait synchronously for in-flight commands on the old tgt_dev to drain; cleanup is deferred to a workqueue. (default: false) (bool)
# options scst async_lun_replace=<value>

