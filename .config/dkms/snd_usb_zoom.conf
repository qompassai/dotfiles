# Generated by confgen.sh
# Module: snd_usb_zoom
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/zoom-l8/0.0.1/7.2.6-zen2-1-zen/x86_64/module/snd-usb-zoom.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p snd_usb_zoom
# Index value for ZOOM L-8 soundcard. (array of int)
# options snd_usb_zoom index=<value>

# ID string for ZOOM L-8 soundcard. (array of charp)
# options snd_usb_zoom id=<value>

# Enable ZOOM L-8 soundcard. (array of bool)
# options snd_usb_zoom enable=<value>

