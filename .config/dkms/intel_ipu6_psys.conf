# Generated by confgen.sh
# Module: intel_ipu6_psys
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/ipu6-drivers/r275.71bddb515/7.2.6-zen2-1-zen/x86_64/module/intel-ipu6-psys.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p intel_ipu6_psys
# enable fw ppg suspend/resume api (bool)
# options intel_ipu6_psys enable_suspend_resume=<value>

# Copy PGs back to user after resource allocation (bool)
# options intel_ipu6_psys early_pg_transfer=<value>

# enable power gating (bool)
# options intel_ipu6_psys enable_power_gating=<value>

# Enable asynchronous firmware initialization (bool)
# options intel_ipu6_psys async_fw_init=<value>

