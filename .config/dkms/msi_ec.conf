# Generated by confgen.sh
# Module: msi_ec
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/msi-ec/r734.d7fbbd8/7.2.6-zen2-1-zen/x86_64/module/msi-ec.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p msi_ec
# Load a configuration for a specified firmware version (charp)
# options msi_ec firmware=<value>

# Load the driver in the debug mode, exporting the debug attributes (bool)
# options msi_ec debug=<value>

