# Generated by confgen.sh
# Module: nvidia_drm
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/nvidia/615.71.09/7.2.6-zen2-1-zen/x86_64/module/nvidia-drm.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options nvidia_drm atomic=1
options nvidia_drm fbdev=1
options nvidia_drm modeset=1

# Available parameters from: modinfo -p nvidia_drm
# Enable atomic kernel modesetting (1 = enable (default), 0 = disable) (bool)
# options nvidia_drm modeset=<value>

# Create a framebuffer device (1 = enable (default), 0 = disable) (bool)
# options nvidia_drm fbdev=<value>

# Enable drm vblank notification support (1 = enable, 0 = disable (default)) (bool)
# options nvidia_drm vblank=<value>

# Enable DRM color pipeline support (1 = enable (default), 0 = disable) (bool)
# options nvidia_drm color_pipeline=<value>

