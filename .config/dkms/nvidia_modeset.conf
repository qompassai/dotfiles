# Generated by confgen.sh
# Module: nvidia_modeset
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/nvidia/615.71.09/7.2.6-zen2-1-zen/x86_64/module/nvidia-modeset.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options nvidia_modeset debug_force_color_space=13
options nvidia_modeset disable_hdmi_frl=0
options nvidia_modeset disable_vrr_memclk_switch=1
options nvidia_modeset enable_overlay_layers=1
options nvidia_modeset hdmi_deepcolor=1
options nvidia_modeset opportunistic_display_sync=1
options nvidia_modeset output_rounding_fix=1
options nvidia_modeset vblank_sem_control=1

# Available parameters from: modinfo -p nvidia_modeset
#  (bool)
# options nvidia_modeset output_rounding_fix=<value>

#  (bool)
# options nvidia_modeset disable_hdmi_frl=<value>

#  (bool)
# options nvidia_modeset disable_vrr_memclk_switch=<value>

#  (bool)
# options nvidia_modeset hdmi_deepcolor=<value>

#  (uint)
# options nvidia_modeset max_output_color_depth=<value>

#  (bool)
# options nvidia_modeset opportunistic_display_sync=<value>

#  (uint)
# options nvidia_modeset debug_force_color_space=<value>

#  (bool)
# options nvidia_modeset enable_overlay_layers=<value>

# Fail the Nth call to nvkms_alloc (int)
# options nvidia_modeset fail_malloc=<value>

# Report information about malloc calls on module unload (bool)
# options nvidia_modeset malloc_verbose=<value>

# Override the default FRL rate selection (2 = max FRL rate w/ DSC, 1 = max FRL rate, 0 = default rate) (int)
# options nvidia_modeset force_frl_rate=<value>

# Conceal all display VRR capabilities (bool)
# options nvidia_modeset conceal_vrr_caps=<value>

# Enable enhanced display protocol converter features (e.g. VRR over active DP-HDMI converters) (bool)
# options nvidia_modeset enhanced_pcon_support=<value>

# Control testing for hardware core channel allocation failure (int)
# options nvidia_modeset fail_alloc_core_channel=<value>

# Enable debug logging (int)
# options nvidia_modeset debug=<value>

# Path to the nvidia-modeset configuration file (default: disabled) (charp)
# options nvidia_modeset config_file=<value>

