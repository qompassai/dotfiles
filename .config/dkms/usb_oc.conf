# Generated by confgen.sh
# Module: usb_oc
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/usb_oc/1.1/7.2.6-zen2-1-zen/x86_64/module/usb_oc.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p usb_oc
# Override the polling interval of all interrupt-type endpoints of a specific USB device by specifying interrupt_interval_override=vendorID:productID:interval.
# options usb_oc interrupt_interval_override=<value>

