# Generated by confgen.sh
# Module: ib_srpt
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/scst-rdma-modules/3.10.5.7r0/7.2.6-zen2-1-zen/x86_64/module/ib_srpt.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options ib_srpt srp_max_req_size=1048576
options ib_srpt srpt_srq_size=4096

# Available parameters from: modinfo -p ib_srpt
# Port number RDMA/CM will bind to. (short)
# options ib_srpt rdma_cm_port=<value>

# Maximum size of SRP RDMA transfers for new connections. (int)
# options ib_srpt srp_max_rdma_size=<value>

# Maximum size of SRP request messages in bytes. (int)
# options ib_srpt srp_max_req_size=<value>

# Maximum size of SRP response messages in bytes. (int)
# options ib_srpt srp_max_rsp_size=<value>

# Whether or not to use SRQ (bool)
# options ib_srpt use_srq=<value>

# Shared receive queue (SRQ) size. (int)
# options ib_srpt srpt_srq_size=<value>

# Per-channel send queue (SQ) size. (int)
# options ib_srpt srpt_sq_size=<value>

# Using this value for ioc_guid, id_ext, and cm_listen_id instead of using the node_guid of the first HCA.
# options ib_srpt srpt_service_guid=<value>

