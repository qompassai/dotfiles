# Generated by confgen.sh
# Module: zfs
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/zfs/2.4.4/7.2.6-zen2-1-zen/x86_64/module/zfs.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p zfs
# Major number for zvol device (uint)
# options zfs zvol_major=<value>

# Max number of blocks to discard (ulong)
# options zfs zvol_max_discard_blocks=<value>

# Default blk-mq queue depth (uint)
# options zfs zvol_blk_mq_queue_depth=<value>

# Use the blk-mq API for zvols (uint)
# options zfs zvol_use_blk_mq=<value>

# Process volblocksize blocks per thread (uint)
# options zfs zvol_blk_mq_blocks_per_thread=<value>

# Timeout for ZVOL open retries (uint)
# options zfs zvol_open_timeout_ms=<value>

# Use legacy ZFS xattr naming for writing new user namespace xattrs
# options zfs zfs_xattr_compat=<value>

# Delete inodes as soon as the last reference is released.
# options zfs zfs_delete_inode=<value>

# Delete dentries from dentry cache as soon as the last reference is released.
# options zfs zfs_delete_dentry=<value>

# Percentage of length to use for the available capacity check (uint)
# options zfs zfs_fallocate_reserve_percent=<value>

# Max number of times a salt value can be used for generating encryption keys before it is rotated (ulong)
# options zfs zfs_key_max_salt_uses=<value>

# Size of znode hold array (uint)
# options zfs zfs_object_mutex_size=<value>

# Set to prevent async unlinks (debug - leaks space into the unlinked set) (int)
# options zfs zfs_unlink_suspend_progress=<value>

# Delete files larger than N blocks async (ulong)
# options zfs zfs_delete_blocks=<value>

# Enable ZFS debug message log (int)
# options zfs zfs_dbgmsg_enable=<value>

# Maximum ZFS debug log size (uint)
# options zfs zfs_dbgmsg_maxsize=<value>

# Enable mkdir/rmdir/mv in .zfs/snapshot (int)
# options zfs zfs_admin_snapshot=<value>

# Seconds to expire .zfs/snapshot (int)
# options zfs zfs_expire_snapshot=<value>

# Disable setuid/setgid for automounts in .zfs/snapshot (int)
# options zfs zfs_snapshot_no_setuid=<value>

# Timeout before determining that a device is missing
# options zfs zfs_vdev_open_timeout_ms=<value>

# Defines failfast mask: 1 - device, 2 - transport, 4 - driver
# options zfs zfs_vdev_failfast_mask=<value>

# Maximum number of data segments to add to an IO request (min 4)
# options zfs zfs_vdev_disk_max_segs=<value>

# Limit on number of pages that ARC shrinker can reclaim at once
# options zfs zfs_arc_shrinker_limit=<value>

# Relative cost of ARC eviction vs other kernel subsystems
# options zfs zfs_arc_shrinker_seeks=<value>

# Toggle whether ABD allocations must be linear. (int)
# options zfs zfs_abd_scatter_enabled=<value>

# Minimum size of scatter allocations. (int)
# options zfs zfs_abd_scatter_min_size=<value>

# Maximum order allocation used for a scatter ABD. (uint)
# options zfs zfs_abd_scatter_max_order=<value>

# Do not create zvol device nodes
# options zfs zvol_inhibit_dev=<value>

# Prefetch N bytes at zvol start+end
# options zfs zvol_prefetch_bytes=<value>

# Default volmode property value
# options zfs zvol_volmode=<value>

# Number of threads for I/O requests. Set to 0 to use all active CPUs
# options zfs zvol_threads=<value>

# Number of zvol taskqs
# options zfs zvol_num_taskqs=<value>

# Synchronously handle bio requests
# options zfs zvol_request_sync=<value>

# Max I/O completion time (milliseconds) before marking it as slow
# options zfs zio_slow_io_ms=<value>

# Prioritize requeued I/O
# options zfs zio_requeue_io_start_cut_in_line=<value>

# Defer frees starting in this pass
# options zfs zfs_sync_pass_deferred_free=<value>

# Don't compress starting in this pass
# options zfs zfs_sync_pass_dont_compress=<value>

# Rewrite new bps starting in this pass
# options zfs zfs_sync_pass_rewrite=<value>

# Throttle block allocations in the ZIO pipeline
# options zfs zio_dva_throttle_enabled=<value>

# Log all slow ZIOs, not just those with vdevs
# options zfs zio_deadman_log_all=<value>

# ZIL block open timeout percentage
# options zfs zfs_commit_timeout_pct=<value>

# Disable intent logging replay
# options zfs zil_replay_disable=<value>

# Disable ZIL cache flushes
# options zfs zil_nocacheflush=<value>

# Limit in bytes slog sync writes per commit
# options zfs zil_slog_bulk=<value>

# Limit in bytes of ZIL log block size
# options zfs zil_maxblocksize=<value>

# Limit in bytes WR_COPIED size
# options zfs zil_maxcopied=<value>

# Largest write size to store data into ZIL
# options zfs zfs_immediate_write_sz=<value>

# Treat special vdevs as SLOG
# options zfs zil_special_is_slog=<value>

# Bytes to read per chunk
# options zfs zfs_vnops_read_chunk_size=<value>

# Enable block cloning
# options zfs zfs_bclone_enabled=<value>

# Wait for dirty blocks when cloning
# options zfs zfs_bclone_wait_dirty=<value>

# Enable Direct I/O
# options zfs zfs_dio_enabled=<value>

# Return errors on misaligned Direct I/O
# options zfs zfs_dio_strict=<value>

# Disable xattr=sa extended attribute logging in ZIL by settng 0.
# options zfs zfs_zil_saxattr=<value>

# Maximum size in bytes allowed for src nvlist passed with ZFS ioctls
# options zfs zfs_max_nvlist_src_size=<value>

# Maximum size in bytes of ZFS ioctl output that will be logged
# options zfs zfs_history_output_max=<value>

# Maximum recent zevents records to retain for duplicate checking
# options zfs zfs_zevent_retain_max=<value>

# Expiration time for recent zevents records
# options zfs zfs_zevent_retain_expire_secs=<value>

# Max instruction limit that can be specified for a channel program
# options zfs zfs_lua_max_instrlimit=<value>

# Max memory limit that can be specified for a channel program
# options zfs zfs_lua_max_memlimit=<value>

# Maximum micro ZAP size before converting to a fat ZAP, in bytes (max 1M)
# options zfs zap_micro_max_size=<value>

# When iterating ZAP object, prefetch it
# options zfs zap_iterate_prefetch=<value>

# Enable ZAP shrinking
# options zfs zap_shrink_enabled=<value>

# Max size of TRIM commands, larger will be split
# options zfs zfs_trim_extent_bytes_max=<value>

# Min size of TRIM commands, smaller will be skipped
# options zfs zfs_trim_extent_bytes_min=<value>

# Skip metaslabs which have never been initialized
# options zfs zfs_trim_metaslab_skip=<value>

# Min number of txgs to aggregate frees before issuing TRIM
# options zfs zfs_trim_txg_batch=<value>

# Max queued TRIMs outstanding per leaf vdev
# options zfs zfs_trim_queue_limit=<value>

# Ignore hard IO errors when removing device
# options zfs zfs_removal_ignore_errors=<value>

# Largest contiguous segment to allocate when removing device
# options zfs zfs_remove_max_segment=<value>

# Largest span of free chunks a remap segment can span
# options zfs vdev_removal_max_span=<value>

# Pause device removal after this many bytes are copied (debug use only - causes removal to hang)
# options zfs zfs_removal_suspend_progress=<value>

# Max segment size in bytes of rebuild reads
# options zfs zfs_rebuild_max_segment=<value>

# Max bytes in flight per leaf vdev for sequential resilvers
# options zfs zfs_rebuild_vdev_limit=<value>

# Automatically scrub after sequential resilver completes
# options zfs zfs_rebuild_scrub_enabled=<value>

# For testing, pause RAIDZ expansion after reflowing this many bytes
# options zfs raidz_expand_max_reflow_bytes=<value>

# Max amount of concurrent i/o for RAIDZ expansion
# options zfs raidz_expand_max_copy_bytes=<value>

# For expanded RAIDZ, aggregate reads that have more rows than this
# options zfs raidz_io_aggregate_rows=<value>

# For expanded RAIDZ, automatically start a pool scrub when expansion completes
# options zfs zfs_scrub_after_expand=<value>

# Raidz/draid slow disk sit out time period in seconds
# options zfs vdev_read_sit_out_secs=<value>

# Interval to check for slow raidz/draid children
# options zfs vdev_raidz_outlier_check_interval_ms=<value>

# How insensitive the slow raidz/draid child check should be
# options zfs vdev_raidz_outlier_insensitivity=<value>

# Max vdev I/O aggregation size
# options zfs zfs_vdev_aggregation_limit=<value>

# Max vdev I/O aggregation size for non-rotating media
# options zfs zfs_vdev_aggregation_limit_non_rotating=<value>

# Aggregate read I/O over gap
# options zfs zfs_vdev_read_gap_limit=<value>

# Aggregate write I/O over gap
# options zfs zfs_vdev_write_gap_limit=<value>

# Maximum number of active I/Os per vdev
# options zfs zfs_vdev_max_active=<value>

# Async write concurrency max threshold
# options zfs zfs_vdev_async_write_active_max_dirty_percent=<value>

# Async write concurrency min threshold
# options zfs zfs_vdev_async_write_active_min_dirty_percent=<value>

# Max active async read I/Os per vdev
# options zfs zfs_vdev_async_read_max_active=<value>

# Min active async read I/Os per vdev
# options zfs zfs_vdev_async_read_min_active=<value>

# Max active async write I/Os per vdev
# options zfs zfs_vdev_async_write_max_active=<value>

# Min active async write I/Os per vdev
# options zfs zfs_vdev_async_write_min_active=<value>

# Max active initializing I/Os per vdev
# options zfs zfs_vdev_initializing_max_active=<value>

# Min active initializing I/Os per vdev
# options zfs zfs_vdev_initializing_min_active=<value>

# Max active removal I/Os per vdev
# options zfs zfs_vdev_removal_max_active=<value>

# Min active removal I/Os per vdev
# options zfs zfs_vdev_removal_min_active=<value>

# Max active scrub I/Os per vdev
# options zfs zfs_vdev_scrub_max_active=<value>

# Min active scrub I/Os per vdev
# options zfs zfs_vdev_scrub_min_active=<value>

# Max active sync read I/Os per vdev
# options zfs zfs_vdev_sync_read_max_active=<value>

# Min active sync read I/Os per vdev
# options zfs zfs_vdev_sync_read_min_active=<value>

# Max active sync write I/Os per vdev
# options zfs zfs_vdev_sync_write_max_active=<value>

# Min active sync write I/Os per vdev
# options zfs zfs_vdev_sync_write_min_active=<value>

# Max active trim/discard I/Os per vdev
# options zfs zfs_vdev_trim_max_active=<value>

# Min active trim/discard I/Os per vdev
# options zfs zfs_vdev_trim_min_active=<value>

# Max active rebuild I/Os per vdev
# options zfs zfs_vdev_rebuild_max_active=<value>

# Min active rebuild I/Os per vdev
# options zfs zfs_vdev_rebuild_min_active=<value>

# Number of non-interactive I/Os to allow in sequence
# options zfs zfs_vdev_nia_credit=<value>

# Number of non-interactive I/Os before _max_active
# options zfs zfs_vdev_nia_delay=<value>

# Rotating media load increment for non-seeking I/Os
# options zfs zfs_vdev_mirror_rotating_inc=<value>

# Rotating media load increment for seeking I/Os
# options zfs zfs_vdev_mirror_rotating_seek_inc=<value>

# Offset in bytes from the last I/O which triggers a reduced rotating media seek increment
# options zfs zfs_vdev_mirror_rotating_seek_offset=<value>

# Non-rotating media load increment for non-seeking I/Os
# options zfs zfs_vdev_mirror_non_rotating_inc=<value>

# Non-rotating media load increment for seeking I/Os
# options zfs zfs_vdev_mirror_non_rotating_seek_inc=<value>

# Value written during zpool initialize
# options zfs zfs_initialize_value=<value>

# Size in bytes of writes by zpool initialize
# options zfs zfs_initialize_chunk_size=<value>

# Whether to attempt condensing indirect vdev mappings
# options zfs zfs_condense_indirect_vdevs_enable=<value>

# Minimum obsolete percent of bytes in the mapping to attempt condensing
# options zfs zfs_condense_indirect_obsolete_pct=<value>

# Don't bother condensing if the mapping uses less than this amount of memory
# options zfs zfs_condense_min_mapping_bytes=<value>

# Minimum size obsolete spacemap to attempt condensing
# options zfs zfs_condense_max_obsolete_bytes=<value>

# Used by tests to ensure certain actions happen in the middle of a condense. A maximum value of 1 should be sufficient.
# options zfs zfs_condense_indirect_commit_entry_delay_ms=<value>

# Maximum number of combinations when reconstructing split segments
# options zfs zfs_reconstruct_indirect_combinations_max=<value>

# Logical ashift for file-based devices
# options zfs vdev_file_logical_ashift=<value>

# Physical ashift for file-based devices
# options zfs vdev_file_physical_ashift=<value>

# Target number of metaslabs per top-level vdev
# options zfs zfs_vdev_default_ms_count=<value>

# Default lower limit for metaslab size
# options zfs zfs_vdev_default_ms_shift=<value>

# Default upper limit for metaslab size
# options zfs zfs_vdev_max_ms_shift=<value>

# Minimum number of metaslabs per top-level vdev
# options zfs zfs_vdev_min_ms_count=<value>

# Practical upper limit of total metaslabs per top-level vdev
# options zfs zfs_vdev_ms_count_limit=<value>

# Rate limit slow IO (delay) events to this many per second
# options zfs zfs_slow_io_events_per_second=<value>

# Rate limit hung IO (deadman) events to this many per second
# options zfs zfs_deadman_events_per_second=<value>

# Rate Direct I/O write verify events to this many per second
# options zfs zfs_dio_write_verify_events_per_second=<value>

# Direct I/O writes will perform for checksum verification before commiting write
# options zfs zfs_vdev_direct_write_verify=<value>

# Rate limit checksum events to this many checksum errors per second (do not set below ZED threshold).
# options zfs zfs_checksum_events_per_second=<value>

# Ignore errors during resilver/scrub
# options zfs zfs_scan_ignore_errors=<value>

# Bypass vdev_validate()
# options zfs vdev_validate_skip=<value>

# Disable cache flushes
# options zfs zfs_nocacheflush=<value>

# Minimum number of metaslabs required to dedicate one for log blocks
# options zfs zfs_embedded_slog_min_ms=<value>

# Minimum ashift used when creating new top-level vdevs
# options zfs zfs_vdev_min_auto_ashift=<value>

# Maximum ashift used when optimizing for logical -> physical sector size on new top-level vdevs
# options zfs zfs_vdev_max_auto_ashift=<value>

# RAIDZ implementation
# options zfs zfs_vdev_raidz_impl=<value>

# Max seconds worth of delta per txg
# options zfs zfs_txg_timeout=<value>

# Historical statistics for the last N reads
# options zfs zfs_read_history=<value>

# Include cache hits in read history
# options zfs zfs_read_history_hits=<value>

# Historical statistics for the last N txgs
# options zfs zfs_txg_history=<value>

# Historical statistics for last N multihost writes
# options zfs zfs_multihost_history=<value>

# Set additional debugging flags
# options zfs zfs_flags=<value>

# Set to attempt to recover from fatal errors
# options zfs zfs_recover=<value>

# Set to ignore IO errors during free and permanently leak the space
# options zfs zfs_free_leak_on_eio=<value>

# Dead I/O check interval in milliseconds
# options zfs zfs_deadman_checktime_ms=<value>

# Enable deadman timer
# options zfs zfs_deadman_enabled=<value>

# SPA size estimate multiplication factor
# options zfs spa_asize_inflation=<value>

# Place DDT data into the special class
# options zfs zfs_ddt_data_is_special=<value>

# Place user data indirect blocks into the special class
# options zfs zfs_user_indirect_is_special=<value>

# Failmode for deadman timer
# options zfs zfs_deadman_failmode=<value>

# Pool sync expiration time in milliseconds
# options zfs zfs_deadman_synctime_ms=<value>

# IO expiration time in milliseconds
# options zfs zfs_deadman_ziotime_ms=<value>

# Small file blocks in special vdevs depends on this much free space available
# options zfs zfs_special_class_metadata_reserve_pct=<value>

# Reserved free space in pool
# options zfs spa_slop_shift=<value>

# Number of allocators per spa
# options zfs spa_num_allocators=<value>

# Minimum number of CPUs per allocators
# options zfs spa_cpus_per_allocator=<value>

# Specific hard-limit in memory that ZFS allows to be used for unflushed changes
# options zfs zfs_unflushed_max_mem_amt=<value>

# Percentage of the overall system memory that ZFS allows to be used for unflushed changes (value is calculated over 1000000 for finer granularity)
# options zfs zfs_unflushed_max_mem_ppm=<value>

# Hard limit (upper-bound) in the size of the space map log in terms of blocks.
# options zfs zfs_unflushed_log_block_max=<value>

# Lower-bound limit for the maximum amount of blocks allowed in log spacemap (see zfs_unflushed_log_block_max)
# options zfs zfs_unflushed_log_block_min=<value>

# Hard limit (upper-bound) in the size of the space map log in terms of dirty TXGs.
# options zfs zfs_unflushed_log_txg_max=<value>

# Tunable used to determine the number of blocks that can be used for the spacemap log, expressed as a percentage of the total number of metaslabs in the pool (e.g. 400 means the number of log blocks is capped at 4 times the number of metaslabs)
# options zfs zfs_unflushed_log_block_pct=<value>

# The number of past TXGs that the flushing algorithm of the log spacemap feature uses to estimate incoming log blocks
# options zfs zfs_max_log_walking=<value>

# Prevent the log spacemaps from being flushed and destroyed during pool export/destroy
# options zfs zfs_keep_log_spacemaps_at_export=<value>

# Maximum number of rows allowed in the summary of the spacemap log
# options zfs zfs_max_logsm_summary_length=<value>

# Minimum number of metaslabs to flush per dirty TXG
# options zfs zfs_min_metaslabs_to_flush=<value>

# Limit the number of errors which will be upgraded to the new on-disk error log when enabling head_errlog
# options zfs spa_upgrade_errlog_limit=<value>

# SPA config file (/etc/zfs/zpool.cache)
# options zfs spa_config_path=<value>

# Limit for memory used in prefetching the checkpoint space map done on each vdev while discarding the checkpoint
# options zfs zfs_spa_discard_memory_limit=<value>

# Percentage of CPUs to run a metaslab preload taskq
# options zfs metaslab_preload_pct=<value>

# log2 fraction of arc that can be used by inflight I/Os when verifying pool during import
# options zfs spa_load_verify_shift=<value>

# Set to traverse metadata on pool import
# options zfs spa_load_verify_metadata=<value>

# Set to traverse data on pool import
# options zfs spa_load_verify_data=<value>

# Print vdev tree to zfs_dbgmsg during pool import
# options zfs spa_load_print_vdev_tree=<value>

# Percentage of CPUs to run an IO worker thread
# options zfs zio_taskq_batch_pct=<value>

# Number of threads per IO worker taskqueue
# options zfs zio_taskq_batch_tpq=<value>

# Allow importing pool with up to this number of missing top-level vdevs (in read-only mode)
# options zfs zfs_max_missing_tvds=<value>

# Set the livelist condense zthr to pause
# options zfs zfs_livelist_condense_zthr_pause=<value>

# Set the livelist condense synctask to pause
# options zfs zfs_livelist_condense_sync_pause=<value>

# Whether livelist condensing was canceled in the synctask
# options zfs zfs_livelist_condense_sync_cancel=<value>

# Whether livelist condensing was canceled in the zthr function
# options zfs zfs_livelist_condense_zthr_cancel=<value>

# Whether extra ALLOC blkptrs were added to a livelist entry while it was being condensed
# options zfs zfs_livelist_condense_new_alloc=<value>

# How frequently TXG timestamps are stored internally (in seconds)
# options zfs spa_note_txg_time=<value>

# How frequently the TXG timestamps database should be flushed to disk (in seconds)
# options zfs spa_flush_txg_time=<value>

# Configure IO queues for read IO
# options zfs zio_taskq_read=<value>

# Configure IO queues for write IO
# options zfs zio_taskq_write=<value>

# Configure IO queues for free IO
# options zfs zio_taskq_free=<value>

# Number of CPUs per write issue taskq
# options zfs zio_taskq_write_tpq=<value>

# Number of sublists used in each multilist
# options zfs zfs_multilist_num_sublists=<value>

# Milliseconds between mmp writes to each leaf
# options zfs zfs_multihost_interval=<value>

# Max allowed period without a successful mmp write
# options zfs zfs_multihost_fail_intervals=<value>

# Number of zfs_multihost_interval periods to wait for activity
# options zfs zfs_multihost_import_intervals=<value>

# Allocation granularity (a.k.a. stripe size)
# options zfs metaslab_aliquot=<value>

# Load all metaslabs when pool is first opened
# options zfs metaslab_debug_load=<value>

# Prevent metaslabs from being unloaded
# options zfs metaslab_debug_unload=<value>

# Preload potential metaslabs during reassessment
# options zfs metaslab_preload_enabled=<value>

# Max number of metaslabs per group to preload
# options zfs metaslab_preload_limit=<value>

# Delay in txgs after metaslab was last used before unloading
# options zfs metaslab_unload_delay=<value>

# Delay in milliseconds after metaslab was last used before unloading
# options zfs metaslab_unload_delay_ms=<value>

# Percentage of metaslab group size that should be free to make it eligible for allocation
# options zfs zfs_mg_noalloc_threshold=<value>

# Percentage of metaslab group size that should be considered eligible for allocations unless all metaslab groups within the metaslab class have also crossed this threshold
# options zfs zfs_mg_fragmentation_threshold=<value>

# Use the fragmentation metric to prefer less fragmented metaslabs
# options zfs metaslab_fragmentation_factor_enabled=<value>

# Fragmentation for metaslab to allow allocation
# options zfs zfs_metaslab_fragmentation_threshold=<value>

# Prefer metaslabs with lower LBAs
# options zfs metaslab_lba_weighting_enabled=<value>

# Enable space-based metaslab group biasing
# options zfs metaslab_bias_enabled=<value>

# Enable performance-based metaslab group biasing
# options zfs metaslab_perf_bias=<value>

# Enable segment-based metaslab selection
# options zfs zfs_metaslab_segment_weight_enabled=<value>

# Segment-based metaslab selection maximum buckets before switching
# options zfs zfs_metaslab_switch_threshold=<value>

# Blocks larger than this size are sometimes forced to be gang blocks
# options zfs metaslab_force_ganging=<value>

# Percentage of large blocks that will be forced to be gang blocks
# options zfs metaslab_force_ganging_pct=<value>

# Max distance (bytes) to search forward before using size tree
# options zfs metaslab_df_max_search=<value>

# When looking in size tree, use largest segment instead of exact fit
# options zfs metaslab_df_use_largest_segment=<value>

# How long to trust the cached max chunk size of a metaslab
# options zfs zfs_metaslab_max_size_cache_sec=<value>

# Percentage of memory that can be used to store metaslab range trees
# options zfs zfs_metaslab_mem_limit=<value>

# Try hard to allocate before ganging
# options zfs zfs_metaslab_try_hard_before_gang=<value>

# Normally only consider this many of the best metaslabs in each vdev
# options zfs zfs_metaslab_find_max_tries=<value>

# SPA active allocator
# options zfs zfs_active_allocator=<value>

# Max event queue length
# options zfs zfs_zevent_len_max=<value>

# Max bytes in flight per leaf vdev for scrubs and resilvers
# options zfs zfs_scan_vdev_limit=<value>

# Min millisecs to scrub per txg
# options zfs zfs_scrub_min_time_ms=<value>

# Min millisecs to obsolete per txg
# options zfs zfs_obsolete_min_time_ms=<value>

# Min millisecs to free per txg
# options zfs zfs_free_min_time_ms=<value>

# Min millisecs to resilver per txg
# options zfs zfs_resilver_min_time_ms=<value>

# Set to prevent scans from progressing
# options zfs zfs_scan_suspend_progress=<value>

# Set to disable scrub I/O
# options zfs zfs_no_scrub_io=<value>

# Set to disable scrub prefetching
# options zfs zfs_no_scrub_prefetch=<value>

# Max number of blocks freed in one txg
# options zfs zfs_async_block_max_blocks=<value>

# Max number of dedup, clone or gang blocks freed in one txg
# options zfs zfs_max_async_dedup_frees=<value>

# Wait for pending free I/Os after issuing this many asynchronously
# options zfs zfs_async_free_zio_wait_interval=<value>

# Enable processing of the free_bpobj
# options zfs zfs_free_bpobj_enabled=<value>

# Enable block statistics calculation during scrub
# options zfs zfs_scan_blkstats=<value>

# Fraction of RAM for scan hard limit
# options zfs zfs_scan_mem_lim_fact=<value>

# IO issuing strategy during scrubbing. 0 = default, 1 = LBA, 2 = size
# options zfs zfs_scan_issue_strategy=<value>

# Scrub using legacy non-sequential method
# options zfs zfs_scan_legacy=<value>

# Number of TXGs to defer background work after pool import
# options zfs zfs_import_defer_txgs=<value>

# Scan progress on-disk checkpointing interval
# options zfs zfs_scan_checkpoint_intval=<value>

# Max gap in bytes between sequential scrub / resilver I/Os
# options zfs zfs_scan_max_ext_gap=<value>

# Fraction of hard limit used as soft limit
# options zfs zfs_scan_mem_lim_soft_fact=<value>

# Tunable to attempt to reduce lock contention
# options zfs zfs_scan_strict_mem_lim=<value>

# Tunable to adjust bias towards more filled segments during scans
# options zfs zfs_scan_fill_weight=<value>

# Tunable to report resilver performance over the last N txgs
# options zfs zfs_scan_report_txgs=<value>

# Process all resilvers immediately
# options zfs zfs_resilver_disable_defer=<value>

# Issued IO percent complete after which resilvers are deferred
# options zfs zfs_resilver_defer_percent=<value>

# Error blocks to be scrubbed in one txg
# options zfs zfs_scrub_error_blocks_per_txg=<value>

# Max percent of RAM allowed to be dirty
# options zfs zfs_dirty_data_max_percent=<value>

# zfs_dirty_data_max upper bound as % of RAM
# options zfs zfs_dirty_data_max_max_percent=<value>

# Transaction delay threshold
# options zfs zfs_delay_min_dirty_percent=<value>

# Determines the dirty space limit
# options zfs zfs_dirty_data_max=<value>

# The size limit of write-transaction zil log data
# options zfs zfs_wrlog_data_max=<value>

# zfs_dirty_data_max upper bound in bytes
# options zfs zfs_dirty_data_max_max=<value>

# Dirty data txg sync threshold as a percentage of zfs_dirty_data_max
# options zfs zfs_dirty_data_sync_percent=<value>

# How quickly delay approaches infinity
# options zfs zfs_delay_scale=<value>

# Max percent of CPUs that are used per dp_sync_taskq
# options zfs zfs_zil_clean_taskq_nthr_pct=<value>

# Number of taskq entries that are pre-populated
# options zfs zfs_zil_clean_taskq_minalloc=<value>

# Max number of taskq entries that are cached
# options zfs zfs_zil_clean_taskq_maxalloc=<value>

# Enable strict ZVOL quota enforcment
# options zfs zvol_enforce_quotas=<value>

# Size to start the next sub-livelist in a livelist
# options zfs zfs_livelist_max_entries=<value>

# Threshold at which livelist is disabled
# options zfs zfs_livelist_min_percent_shared=<value>

# Max allowed record size
# options zfs zfs_max_recordsize=<value>

# Allow mounting of redacted datasets
# options zfs zfs_allow_redacted_dataset_mount=<value>

# Include snapshot events in pool history/events
# options zfs zfs_snapshot_history_enabled=<value>

# Set to allow raw receives without IVset guids
# options zfs zfs_disable_ivset_guid_check=<value>

# Default dnode block shift
# options zfs zfs_default_bs=<value>

# Default dnode indirect block shift
# options zfs zfs_default_ibs=<value>

# Disable all ZFS prefetching
# options zfs zfs_prefetch_disable=<value>

# Max number of streams per zfetch
# options zfs zfetch_max_streams=<value>

# Min time before stream reclaim
# options zfs zfetch_min_sec_reap=<value>

# Max time before stream delete
# options zfs zfetch_max_sec_reap=<value>

# Min bytes to prefetch per stream
# options zfs zfetch_min_distance=<value>

# Max bytes to prefetch per stream
# options zfs zfetch_max_distance=<value>

# Max bytes to prefetch indirects for per stream
# options zfs zfetch_max_idistance=<value>

# Max request reorder distance within a stream
# options zfs zfetch_max_reorder=<value>

# Max log2 fraction of holes in a stream
# options zfs zfetch_hole_shift=<value>

# Max number of bytes to prefetch
# options zfs zfs_pd_bytes_max=<value>

# Traverse prefetch number of blocks pointed by indirect block
# options zfs zfs_traverse_indirect_prefetch_limit=<value>

# Ignore hole_birth txg for zfs send
# options zfs send_holes_without_birth_time=<value>

# Allow sending corrupt data
# options zfs zfs_send_corrupt_data=<value>

# Maximum send queue length
# options zfs zfs_send_queue_length=<value>

# Send unmodified spill blocks
# options zfs zfs_send_unmodified_spill_blocks=<value>

# Maximum send queue length for non-prefetch queues
# options zfs zfs_send_no_prefetch_queue_length=<value>

# Send queue fill fraction
# options zfs zfs_send_queue_ff=<value>

# Send queue fill fraction for non-prefetch queues
# options zfs zfs_send_no_prefetch_queue_ff=<value>

# Override block size estimate with fixed size
# options zfs zfs_override_estimate_recordsize=<value>

# Maximum receive queue length
# options zfs zfs_recv_queue_length=<value>

# Receive queue fill fraction
# options zfs zfs_recv_queue_ff=<value>

# Maximum amount of writes to batch into one transaction
# options zfs zfs_recv_write_batch_size=<value>

# Ignore errors during corrective receive
# options zfs zfs_recv_best_effort_corrective=<value>

# CPU-specific allocator grabs 2^N objects at once
# options zfs dmu_object_alloc_chunk_shift=<value>

# Enable NOP writes
# options zfs zfs_nopwrite_enabled=<value>

# Percentage of dirtied blocks from frees in one TXG
# options zfs zfs_per_txg_dirty_frees_percent=<value>

# Enable forcing txg sync to find holes
# options zfs zfs_dmu_offset_next_sync=<value>

# Limit one prefetch call to this size
# options zfs dmu_prefetch_max=<value>

# Override copies= for dedup objects
# options zfs dmu_ddt_copies=<value>

# DDT ZAP leaf blockshift
# options zfs ddt_zap_default_bs=<value>

# DDT ZAP indirect blockshift
# options zfs ddt_zap_default_ibs=<value>

# Max transactions before starting to flush dedup logs
# options zfs zfs_dedup_log_txg_max=<value>

# Max memory for dedup logs
# options zfs zfs_dedup_log_mem_max=<value>

# Max memory for dedup logs, as % of total memory
# options zfs zfs_dedup_log_mem_max_percent=<value>

# Enable prefetching dedup-ed blks
# options zfs zfs_dedup_prefetch=<value>

# Min time to spend on incremental dedup log flush each transaction
# options zfs zfs_dedup_log_flush_min_time_ms=<value>

# Min number of log entries to flush each transaction
# options zfs zfs_dedup_log_flush_entries_min=<value>

# Max number of log entries to flush each transaction
# options zfs zfs_dedup_log_flush_entries_max=<value>

# Number of TXGs to try to rotate the log in
# options zfs zfs_dedup_log_flush_txgs=<value>

# Soft cap for the size of the current dedup log
# options zfs zfs_dedup_log_cap=<value>

# Whether to use the soft cap as a hard cap
# options zfs zfs_dedup_log_hard_cap=<value>

# Number of txgs to average flow rates across
# options zfs zfs_dedup_log_flush_flow_rate_txgs=<value>

# Calculate arc header index
# options zfs zfs_dbuf_state_index=<value>

# Maximum size in bytes of the dbuf cache.
# options zfs dbuf_cache_max_bytes=<value>

# Percentage over dbuf_cache_max_bytes for direct dbuf eviction.
# options zfs dbuf_cache_hiwater_pct=<value>

# Percentage below dbuf_cache_max_bytes when dbuf eviction stops.
# options zfs dbuf_cache_lowater_pct=<value>

# Maximum size in bytes of dbuf metadata cache.
# options zfs dbuf_metadata_cache_max_bytes=<value>

# Set size of dbuf cache to log2 fraction of arc size.
# options zfs dbuf_cache_shift=<value>

# Set size of dbuf metadata cache to log2 fraction of arc size.
# options zfs dbuf_metadata_cache_shift=<value>

# Set size of dbuf cache mutex array as log2 shift.
# options zfs dbuf_mutex_cache_shift=<value>

# Enable btree verification. Levels above 4 require ZFS be built with debugging
# options zfs zfs_btree_verify_intensity=<value>

# Enable prefetching of BRT ZAP entries
# options zfs brt_zap_prefetch=<value>

# BRT ZAP leaf blockshift
# options zfs brt_zap_default_bs=<value>

# BRT ZAP indirect blockshift
# options zfs brt_zap_default_ibs=<value>

# Minimum ARC size in bytes
# options zfs zfs_arc_min=<value>

# Maximum ARC size in bytes
# options zfs zfs_arc_max=<value>

# Balance between metadata and data on ghost hits.
# options zfs zfs_arc_meta_balance=<value>

# Seconds before growing ARC size
# options zfs zfs_arc_grow_retry=<value>

# log2(fraction of ARC to reclaim)
# options zfs zfs_arc_shrink_shift=<value>

# Percent of pagecache to reclaim ARC to
# options zfs zfs_arc_pc_percent=<value>

# Target average block size
# options zfs zfs_arc_average_blocksize=<value>

# Disable compressed ARC buffers
# options zfs zfs_compressed_arc_enabled=<value>

# Min life of prefetch block in ms
# options zfs zfs_arc_min_prefetch_ms=<value>

# Min life of prescient prefetched block in ms
# options zfs zfs_arc_min_prescient_prefetch_ms=<value>

# Max write bytes per interval
# options zfs l2arc_write_max=<value>

# Extra write bytes during device warmup
# options zfs l2arc_write_boost=<value>

# Number of max device writes to precache
# options zfs l2arc_headroom=<value>

# Compressed l2arc_headroom multiplier
# options zfs l2arc_headroom_boost=<value>

# TRIM ahead L2ARC write size multiplier
# options zfs l2arc_trim_ahead=<value>

# Seconds between L2ARC writing
# options zfs l2arc_feed_secs=<value>

# Min feed interval in milliseconds
# options zfs l2arc_feed_min_ms=<value>

# Skip caching prefetched buffers
# options zfs l2arc_noprefetch=<value>

# Turbo L2ARC warmup
# options zfs l2arc_feed_again=<value>

# No reads during writes
# options zfs l2arc_norw=<value>

# Percent of ARC size allowed for L2ARC-only headers
# options zfs l2arc_meta_percent=<value>

# Rebuild the L2ARC when importing a pool
# options zfs l2arc_rebuild_enabled=<value>

# Min size in bytes to write rebuild log blocks in L2ARC
# options zfs l2arc_rebuild_blocks_min_l2size=<value>

# Cache only MFU data from ARC into L2ARC
# options zfs l2arc_mfuonly=<value>

# Exclude dbufs on special vdevs from being cached to L2ARC if set.
# options zfs l2arc_exclude_special=<value>

# System free memory I/O throttle in bytes
# options zfs zfs_arc_lotsfree_percent=<value>

# System free memory target size in bytes
# options zfs zfs_arc_sys_free=<value>

# Minimum bytes of dnodes in ARC
# options zfs zfs_arc_dnode_limit=<value>

# Percent of ARC meta buffers for dnodes
# options zfs zfs_arc_dnode_limit_percent=<value>

# Percentage of excess dnodes to try to unpin
# options zfs zfs_arc_dnode_reduce_percent=<value>

# When full, ARC allocation waits for eviction of this % of alloc size
# options zfs zfs_arc_eviction_pct=<value>

# The number of headers to evict per sublist before moving to the next
# options zfs zfs_arc_evict_batch_limit=<value>

# The number of batches to run per parallel eviction task
# options zfs zfs_arc_evict_batches_limit=<value>

# Number of arc_prune threads
# options zfs zfs_arc_prune_task_threads=<value>

# Number of threads to use for ARC eviction.
# options zfs zfs_arc_evict_threads=<value>

# Enable early abort attempts when using zstd
# options zfs zstd_earlyabort_pass=<value>

# Minimal size of block to attempt early abort
# options zfs zstd_abort_size=<value>

# Limit to the amount of nesting a path can have. Defaults to 50.
# options zfs zfs_max_dataset_nesting=<value>

# Select fletcher 4 implementation.
# options zfs zfs_fletcher_4_impl=<value>

# Select SHA512 implementation.
# options zfs zfs_sha512_impl=<value>

# Select SHA256 implementation.
# options zfs zfs_sha256_impl=<value>

# Select gcm implementation.
# options zfs icp_gcm_impl=<value>

# How many bytes to process while owning the FPU
# options zfs icp_gcm_avx_chunk_size=<value>

# Select BLAKE3 implementation.
# options zfs zfs_blake3_impl=<value>

# Select aes implementation.
# options zfs icp_aes_impl=<value>

