# Generated by confgen.sh
# Module: snd_usb_audio_fasttrack
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/snd-usb-audio-fasttrack/1/7.2.6-zen2-1-zen/x86_64/module/snd-usb-audio-fasttrack.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p snd_usb_audio_fasttrack
# Enable MIDI 2.0 support. (bool)
# options snd_usb_audio_fasttrack midi2_enable=<value>

# Probe UMP v1.1 support at first. (bool)
# options snd_usb_audio_fasttrack midi2_ump_probe=<value>

# Index value for the USB audio adapter. (array of int)
# options snd_usb_audio_fasttrack index=<value>

# ID string for the USB audio adapter. (array of charp)
# options snd_usb_audio_fasttrack id=<value>

# Enable USB audio adapter. (array of bool)
# options snd_usb_audio_fasttrack enable=<value>

# Vendor ID for the USB audio device. (array of int)
# options snd_usb_audio_fasttrack vid=<value>

# Product ID for the USB audio device. (array of int)
# options snd_usb_audio_fasttrack pid=<value>

# Specific device setup (if needed). (array of int)
# options snd_usb_audio_fasttrack device_setup=<value>

# Ignore errors from USB controller for mixer interfaces. (bool)
# options snd_usb_audio_fasttrack ignore_ctl_error=<value>

# Enable auto-clock selection for UAC2 devices (default: yes). (bool)
# options snd_usb_audio_fasttrack autoclock=<value>

# Enable low latency playback (default: yes). (bool)
# options snd_usb_audio_fasttrack lowlatency=<value>

# Quirk aliases, e.g. 0123abcd:5678beef. (array of charp)
# options snd_usb_audio_fasttrack quirk_alias=<value>

# Quirk for delayed registration, given by id:iface, e.g. 0123abcd:4. (array of charp)
# options snd_usb_audio_fasttrack delayed_register=<value>

# Apply generic implicit feedback sync mode. (array of bool)
# options snd_usb_audio_fasttrack implicit_fb=<value>

# Use vmalloc for PCM intermediate buffers (default: yes). (bool)
# options snd_usb_audio_fasttrack use_vmalloc=<value>

# Skip unit descriptor validation (default: no). (bool)
# options snd_usb_audio_fasttrack skip_validation=<value>

# Add/modify USB audio quirks (array of quirkp)
# options snd_usb_audio_fasttrack quirk_flags=<value>

