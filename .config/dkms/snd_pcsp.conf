# Generated by confgen.sh
# Module: snd_pcsp
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/snd-pcsp/6.18.9/7.2.6-zen2-1-zen/x86_64/module/snd-pcsp.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p snd_pcsp
# Apply NForce chipset workaround (expect bad sound) (bool)
# options snd_pcsp nforce_wa=<value>

# Index value for pcsp soundcard. (int)
# options snd_pcsp index=<value>

# ID string for pcsp soundcard. (charp)
# options snd_pcsp id=<value>

# Enable PC-Speaker sound. (bool)
# options snd_pcsp enable=<value>

# Disable PC-Speaker PCM sound. Only beeps remain. (bool)
# options snd_pcsp nopcm=<value>

