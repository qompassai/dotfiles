# Generated by confgen.sh
# Module: xt_pknock
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/xtables-addons/3.31/7.2.6-zen2-1-zen/x86_64/module/xt_pknock.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p xt_pknock
# Buckets in rule hash table (default: 8) (int)
# options xt_pknock rule_hashsize=<value>

# Buckets in peer hash table (default: 16) (int)
# options xt_pknock peer_hashsize=<value>

# Time until garbage collection after valid knock packet (default: 65000 msec) (int)
# options xt_pknock gc_expir_time=<value>

# Netlink multicast group number for pknock messages (int)
# options xt_pknock nl_multicast_group=<value>

