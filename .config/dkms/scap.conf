# Generated by confgen.sh
# Module: scap
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/scap/9.1.0/7.2.6-zen2-1-zen/x86_64/module/scap.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p scap
# This is the dimension of a single per-CPU buffer in bytes. Please note: this buffer will be mapped twice in the process virtual memory, so pay attention to its size.
# options scap g_buffer_bytes_dim=<value>

# Maximum number of consumers that can simultaneously open the devices (uint)
# options scap max_consumers=<value>

# Enable verbose logging (bool)
# options scap verbose=<value>

