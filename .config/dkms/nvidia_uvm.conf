# Generated by confgen.sh
# Module: nvidia_uvm
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/nvidia/615.71.09/7.2.6-zen2-1-zen/x86_64/module/nvidia-uvm.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options nvidia_uvm uvm_disable_hmm=0
options nvidia_uvm uvm_enable_va_space_mm=1
options nvidia_uvm uvm_global_oversubscription=1
options nvidia_uvm uvm_leak_checker=2
options nvidia_uvm uvm_page_table_location=vid
options nvidia_uvm uvm_peer_copy=virt
options nvidia_uvm uvm_perf_access_counter_batch_count=32
options nvidia_uvm uvm_perf_access_counter_migration_enable=1
options nvidia_uvm uvm_perf_access_counter_threshold=100
options nvidia_uvm uvm_perf_fault_batch_count=64
options nvidia_uvm uvm_perf_fault_coalesce=1
options nvidia_uvm uvm_perf_prefetch_enable=1
options nvidia_uvm uvm_perf_prefetch_min_faults=2
options nvidia_uvm uvm_perf_prefetch_threshold=4
options nvidia_uvm uvm_perf_thrashing_enable=0

# Available parameters from: modinfo -p nvidia_uvm
#  (ulong)
# options nvidia_uvm uvm_conf_computing_channel_iv_rotation_limit=<value>

# Set to 0 to disable ATS (Address Translation Services). Any other value is ignored. Has no effect unless the platform supports ATS. (int)
# options nvidia_uvm uvm_ats_mode=<value>

#  (uint)
# options nvidia_uvm uvm_perf_prefetch_enable=<value>

#  (uint)
# options nvidia_uvm uvm_perf_prefetch_threshold=<value>

#  (uint)
# options nvidia_uvm uvm_perf_prefetch_min_faults=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_enable=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_threshold=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_pin_threshold=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_lapse_usec=<value>

#  (uint)
# options nvidia_uvm uvm_perf_lapse_vote_threshold=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_nap=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_epoch=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_pin=<value>

#  (uint)
# options nvidia_uvm uvm_perf_thrashing_max_resets=<value>

#  (uint)
# options nvidia_uvm uvm_perf_map_remote_on_native_atomics_fault=<value>

# Force-disable HMM functionality in the UVM driver. Default: false (HMM is enabled if possible). However, even with uvm_disable_hmm=false, HMM will not be enabled if is not supported in this driver build configuration, or if ATS settings conflict with HMM. (bool)
# options nvidia_uvm uvm_disable_hmm=<value>

#  (int)
# options nvidia_uvm uvm_perf_migrate_cpu_preunmap_enable=<value>

#  (uint)
# options nvidia_uvm uvm_perf_migrate_cpu_preunmap_block_order=<value>

# Enable (1) or disable (0) global oversubscription support. (int)
# options nvidia_uvm uvm_global_oversubscription=<value>

#  (uint)
# options nvidia_uvm uvm_perf_pma_batch_nonpinned_order=<value>

# OR'ed value of all CPU chunk allocation sizes. (uint)
# options nvidia_uvm uvm_cpu_chunk_allocation_sizes=<value>

# Enable uvm memory leak checking. 0 = disabled, 1 = count total bytes allocated and freed, 2 = per-allocation origin tracking. (int)
# options nvidia_uvm uvm_leak_checker=<value>

# Enable push description tracking (uint)
# options nvidia_uvm uvm_debug_enable_push_desc=<value>

# Enable push acquire information tracking (uint)
# options nvidia_uvm uvm_debug_enable_push_acquire_info=<value>

# Set the location for UVM-allocated page tables. Choices are: vid, sys. (charp)
# options nvidia_uvm uvm_page_table_location=<value>

# Whether access counters will trigger migrations.Valid values: <= -1 (default policy), 0 (off), >= 1 (on) (int)
# options nvidia_uvm uvm_perf_access_counter_migration_enable=<value>

#  (uint)
# options nvidia_uvm uvm_perf_access_counter_batch_count=<value>

# Number of remote accesses on a region required to trigger a notification.Valid values: [1, 65535] (uint)
# options nvidia_uvm uvm_perf_access_counter_threshold=<value>

#  (uint)
# options nvidia_uvm uvm_perf_reenable_prefetch_faults_lapse_msec=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_batch_count=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_replay_policy=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_replay_update_put_ratio=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_max_batches_per_service=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_max_throttle_per_service=<value>

#  (uint)
# options nvidia_uvm uvm_perf_fault_coalesce=<value>

# Force (1) using sysmem storage for pages that faulted. Default: 0. (int)
# options nvidia_uvm uvm_fault_force_sysmem=<value>

#  (int)
# options nvidia_uvm uvm_perf_map_remote_on_eviction=<value>

# Use GPU CEs for CPU-to-CPU migrations. (int)
# options nvidia_uvm uvm_block_cpu_to_cpu_copy_with_ce=<value>

# Force caching for mappings to peer memory. This is an experimental parameter that may cause correctness issues if used. (uint)
# options nvidia_uvm uvm_exp_gpu_cache_peermem=<value>

# Force caching for mappings to system memory. This is an experimental parameter that may cause correctness issues if used. (uint)
# options nvidia_uvm uvm_exp_gpu_cache_sysmem=<value>

# Force all TLB invalidation downgrades to use MEMBAR_SYS (uint)
# options nvidia_uvm uvm_downgrade_force_membar_sys=<value>

#  (uint)
# options nvidia_uvm uvm_channel_num_gpfifo_entries=<value>

#  (charp)
# options nvidia_uvm uvm_channel_gpfifo_loc=<value>

#  (charp)
# options nvidia_uvm uvm_channel_gpput_loc=<value>

#  (charp)
# options nvidia_uvm uvm_channel_pushbuffer_loc=<value>

# Set to 0 to disable UVM from using mmu_notifiers to create an association between a UVM VA space and a process. This will also disable pageable memory access via either ATS or HMM. (int)
# options nvidia_uvm uvm_enable_va_space_mm=<value>

# Disable migration of system allocated memory for CDMM/HMM Default: true (migration is disabled).  (bool)
# options nvidia_uvm uvm_disable_sam_migration=<value>

# Enable debug procfs entries in /proc/driver/nvidia-uvm (int)
# options nvidia_uvm uvm_enable_debug_procfs=<value>

# Choose the addressing mode for peer copying, options: phys [default] or virt. Valid for Ampere+ GPUs. (charp)
# options nvidia_uvm uvm_peer_copy=<value>

#  (uint)
# options nvidia_uvm uvm_force_conf_computing=<value>

# Enable uvm debug prints. (int)
# options nvidia_uvm uvm_debug_prints=<value>

# Enable the UVM built-in tests. (This is a security risk) (int)
# options nvidia_uvm uvm_enable_builtin_tests=<value>

# Enable uvm asserts included in release builds. (int)
# options nvidia_uvm uvm_release_asserts=<value>

# dump_stack() on failed UVM release asserts. (int)
# options nvidia_uvm uvm_release_asserts_dump_stack=<value>

# Set UVM global fatal error on failed release asserts. (int)
# options nvidia_uvm uvm_release_asserts_set_global_error=<value>

