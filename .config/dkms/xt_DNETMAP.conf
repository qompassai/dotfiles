# Generated by confgen.sh
# Module: xt_DNETMAP
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/xtables-addons/3.31/7.2.6-zen2-1-zen/x86_64/module/xt_DNETMAP.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p xt_DNETMAP
#  default ttl value to be used if rule doesn't specify any (default: 600) (uint)
# options xt_DNETMAP default_ttl=<value>

#  hash size for ip lists, needs to be power of 2 (default: 256) (uint)
# options xt_DNETMAP hash_size=<value>

#  disables logging of bind/timeout events (default: 0) (uint)
# options xt_DNETMAP disable_log=<value>

#  use network and broadcast addresses of specified prefix for bindings (default: 1) (uint)
# options xt_DNETMAP whole_prefix=<value>

