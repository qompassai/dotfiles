# Generated by confgen.sh
# Module: vtunerc
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/vtunerc/1.4/7.2.6-zen2-1-zen/x86_64/module/vtunerc.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p vtunerc
# DVB adapter numbers (array of short)
# options vtunerc adapter_nr=<value>

# Number of virtual adapters (default is 1) (int)
# options vtunerc devices=<value>

# Check TS packet validity (default is 0) (int)
# options vtunerc tscheck=<value>

# Enable debug messages (default is 0) (int)
# options vtunerc debug=<value>

