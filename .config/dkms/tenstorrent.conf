# Generated by confgen.sh
# Module: tenstorrent
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/tt-kmd/2.11.0/7.2.6-zen2-1-zen/x86_64/module/tenstorrent.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options tenstorrent auto_reset_timeout=10
options tenstorrent dma_address_bits=0
options tenstorrent max_devices=4
options tenstorrent reset_limit=10

# Available parameters from: modinfo -p tenstorrent
# Maximum number of tenstorrent devices (chips) to support. (uint)
# options tenstorrent max_devices=<value>

# DMA address bits, 0 for automatic. (uint)
# options tenstorrent dma_address_bits=<value>

# Maximum number of times to reset device during boot. (uint)
# options tenstorrent reset_limit=<value>

# Timeout duration in seconds for M3 auto reset to occur. (byte)
# options tenstorrent auto_reset_timeout=<value>

# Enable power policy: low power at probe, re-aggregate on close (default=on). (bool)
# options tenstorrent power_policy=<value>

# Forward Blackhole firmware log messages to the kernel log (default=on). (bool)
# options tenstorrent fw_logging=<value>

# Maximum firmware log level forwarded to the kernel log: 1=error, 2=warning, 3=info, 4=debug (default=2). (uint)
# options tenstorrent fw_log_level=<value>

# Delay in ms between the last fd closing a device and the idle power-down message being sent.  0 sends the message synchronously at close.  Only honored by device classes that opt in via defer_idle_powerdown. (uint)
# options tenstorrent idle_power_down_grace_ms=<value>

