# Generated by confgen.sh
# Module: spl
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/zfs/2.4.4/7.2.6-zen2-1-zen/x86_64/module/spl.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p spl
# Bind taskq thread to CPU by default (int)
# options spl spl_taskq_thread_bind=<value>

# Minimum idle threads exit interval for dynamic taskqs (uint)
# options spl spl_taskq_thread_timeout_ms=<value>

# Allow dynamic taskq threads (int)
# options spl spl_taskq_thread_dynamic=<value>

# Allow non-default priority for taskq threads (int)
# options spl spl_taskq_thread_priority=<value>

# Create new taskq threads after N sequential tasks (uint)
# options spl spl_taskq_thread_sequential=<value>

# Write nonzero to kick stuck taskqs to spawn more threads
# options spl spl_taskq_kick=<value>

# Warning threshold in bytes for a kmem_alloc() (uint)
# options spl spl_kmem_alloc_warn=<value>

# Maximum size in bytes for a kmem_alloc() (uint)
# options spl spl_kmem_alloc_max=<value>

# Default magazine size (2-256), set automatically (0) (uint)
# options spl spl_kmem_cache_magazine_size=<value>

# Number of objects per slab (uint)
# options spl spl_kmem_cache_obj_per_slab=<value>

# Maximum size of slab in MB (uint)
# options spl spl_kmem_cache_max_size=<value>

# Objects less than N bytes use the Linux slab (uint)
# options spl spl_kmem_cache_slab_limit=<value>

# Number of spl_kmem_cache threads (uint)
# options spl spl_kmem_cache_kmem_threads=<value>

# The system hostid. (ulong)
# options spl spl_hostid=<value>

# The system hostid file (/etc/hostid) (charp)
# options spl spl_hostid_path=<value>

# Cause kernel panic on assertion failures (uint)
# options spl spl_panic_halt=<value>

# schedule_hrtimeout_range() delta/slack value in us, default(0)
# options spl spl_schedule_hrtimeout_slack_us=<value>

