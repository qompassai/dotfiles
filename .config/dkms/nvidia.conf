# Generated by confgen.sh
# Module: nvidia
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/nvidia/615.71.09/7.2.6-zen2-1-zen/x86_64/module/nvidia.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration
options nvidia NVreg_PreserveVideoMemoryAllocations=1
options nvidia NVreg_UseKernelSuspendNotifiers=1
options nvidia NVreg_TemporaryFilePath=/var/tmp
options nvidia NVreg_DeviceFileMode=0666
options nvidia NVreg_DynamicPowerManagement=2
options nvidia NVreg_DynamicPowerManagementVideoMemoryThreshold=2000
options nvidia NVreg_EnableGpuFirmware=0
options nvidia NVreg_EnablePCIERelaxedOrderingMode=1
options nvidia NVreg_EnableResizableBar=1
options nvidia NVreg_EnableS0ixPowerManagement=1
options nvidia NVreg_EnableStreamMemOPs=1
options nvidia NVreg_IgnoreMMIOCheck=1
options nvidia NVreg_InitializeSystemMemoryAllocations=0
options nvidia NVreg_OpenRmEnableUnsupportedGpus=1
options nvidia NVreg_PreserveVideoMemoryAllocations=1
options nvidia NVreg_RestrictProfilingToAdminUsers=0
options nvidia NVreg_TemporaryFilePath=/var/tmp
options nvidia NVreg_UsePageAttributeTable=1

# Available parameters from: modinfo -p nvidia
# NvSwitch regkey (charp)
# options nvidia NvSwitchRegDwords=<value>

# NvSwitchBlacklist=uuid[,uuid...] (charp)
# options nvidia NvSwitchBlacklist=<value>

#  (int)
# options nvidia NVreg_ResmanDebugLevel=<value>

#  (int)
# options nvidia NVreg_RmLogonRC=<value>

#  (int)
# options nvidia NVreg_ModifyDeviceFiles=<value>

#  (int)
# options nvidia NVreg_DeviceFileUID=<value>

#  (int)
# options nvidia NVreg_DeviceFileGID=<value>

#  (int)
# options nvidia NVreg_DeviceFileMode=<value>

#  (int)
# options nvidia NVreg_InitializeSystemMemoryAllocations=<value>

#  (int)
# options nvidia NVreg_EnablePCIeGen3=<value>

#  (int)
# options nvidia NVreg_EnableMSI=<value>

#  (int)
# options nvidia NVreg_EnableStreamMemOPs=<value>

#  (int)
# options nvidia NVreg_RestrictProfilingToAdminUsers=<value>

#  (int)
# options nvidia NVreg_PreserveVideoMemoryAllocations=<value>

#  (int)
# options nvidia NVreg_EnableS0ixPowerManagement=<value>

#  (int)
# options nvidia NVreg_S0ixPowerManagementVideoMemoryThreshold=<value>

#  (int)
# options nvidia NVreg_DynamicPowerManagement=<value>

#  (int)
# options nvidia NVreg_DynamicPowerManagementVideoMemoryThreshold=<value>

#  (int)
# options nvidia NVreg_EnableGpuFirmware=<value>

#  (int)
# options nvidia NVreg_EnableGpuFirmwareLogs=<value>

#  (int)
# options nvidia NVreg_OpenRmEnableUnsupportedGpus=<value>

#  (int)
# options nvidia NVreg_EnableUserNUMAManagement=<value>

#  (int)
# options nvidia NVreg_MemoryPoolSize=<value>

#  (int)
# options nvidia NVreg_KMallocHeapMaxSize=<value>

#  (int)
# options nvidia NVreg_VMallocHeapMaxSize=<value>

#  (int)
# options nvidia NVreg_IgnoreMMIOCheck=<value>

#  (int)
# options nvidia NVreg_NvLinkDisable=<value>

#  (int)
# options nvidia NVreg_EnablePCIERelaxedOrderingMode=<value>

#  (int)
# options nvidia NVreg_RegisterPCIDriver=<value>

#  (int)
# options nvidia NVreg_RegisterPlatformDeviceDriver=<value>

#  (int)
# options nvidia NVreg_EnableResizableBar=<value>

#  (int)
# options nvidia NVreg_EnableDbgBreakpoint=<value>

#  (int)
# options nvidia NVreg_TegraGpuPgMask=<value>

#  (int)
# options nvidia NVreg_EnableNonblockingOpen=<value>

#  (int)
# options nvidia NVreg_ExcludeAllGpus=<value>

#  (int)
# options nvidia NVreg_GpuInitOnProbe=<value>

#  (charp)
# options nvidia NVreg_CoherentGPUMemoryMode=<value>

#  (charp)
# options nvidia NVreg_RegistryDwords=<value>

#  (charp)
# options nvidia NVreg_RegistryDwordsPerDevice=<value>

#  (charp)
# options nvidia NVreg_RegistryBinaryFilePerDevice=<value>

#  (charp)
# options nvidia NVreg_RmMsg=<value>

#  (charp)
# options nvidia NVreg_GpuBlacklist=<value>

#  (charp)
# options nvidia NVreg_TemporaryFilePath=<value>

#  (charp)
# options nvidia NVreg_ExcludedGpus=<value>

#  (int)
# options nvidia NVreg_DmaRemapPeerMmio=<value>

#  (charp)
# options nvidia NVreg_RmNvlinkBandwidth=<value>

#  (int)
# options nvidia NVreg_RmNvlinkBandwidthLinkCount=<value>

#  (int)
# options nvidia NVreg_ImexChannelCount=<value>

#  (int)
# options nvidia NVreg_CreateImexChannel0=<value>

#  (int)
# options nvidia NVreg_EnableSystemMemoryPools=<value>

#  (int)
# options nvidia NVreg_OsEnableCxlSupport=<value>

#  (int)
# options nvidia NVreg_UseKernelSuspendNotifiers=<value>

#  (int)
# options nvidia NVreg_EnableDebuggerInterface=<value>

#  (charp)
# options nvidia rm_firmware_active=<value>

