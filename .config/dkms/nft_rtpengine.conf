# Generated by confgen.sh
# Module: nft_rtpengine
# Kernel: 7.2.6-zen2-1-zen
# DKMS artifact: /var/lib/dkms/rtpengine/26.2.1.2/7.2.6-zen2-1-zen/x86_64/module/nft_rtpengine.ko.zst
#
# Uncomment and replace <value> only for parameters you intend
# to set. Do not copy every option blindly: module defaults are
# usually preferable unless you have a specific reason to override.

# Current effective modprobe configuration: none found

# Available parameters from: modinfo -p nft_rtpengine
# rtpengine procfs tree user id (uint)
# options nft_rtpengine proc_uid=<value>

# rtpengine procfs tree group id (uint)
# options nft_rtpengine proc_gid=<value>

# rtpengine procfs tree mode mask (hexint)
# options nft_rtpengine proc_mask=<value>

# maximum number of packets to retain for intercept streams (uint)
# options nft_rtpengine stream_packets_list_limit=<value>

# generate kernel log lines from forwarding errors (bool)
# options nft_rtpengine log_errors=<value>

# log verbose errors for incorrect API usage (EINVAL errors) (bool)
# options nft_rtpengine api_debug=<value>

